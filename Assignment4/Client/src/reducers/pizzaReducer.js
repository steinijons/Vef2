import { GET_ALL_PIZZAS } from '../constants/pizzaConstants';

const pizzaReducer = (state =[], action) => {
    switch (atcion.type) {
        case GET_ALL_PIZZAS: return state;
        default: return state;
    }
};

export default pizzaReducer;
